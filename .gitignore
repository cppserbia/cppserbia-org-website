# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env*

# private keys
*.pem

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# images (hosted on R2, not in git)
/images/events/

# banner templates: held in R2, fetched on demand into the cache dir; the
# `templates/` dir is the local staging area for uploads (see
# scripts/banner/README.md).
scripts/banner/templates/
scripts/banner/.template-cache/